name: env
on: workflow_dispatch
jobs:
    mainjob:
        name: windows env
        runs-on: windows-latest
        steps:
            - name: win
              run: env
    secondjob:
        name: linux env
        runs-on: ubuntu-latest
        steps:
            - name: linux
              run: env
    thirdjob:
        name: mac env
        runs-on: macos-latest
        steps:
            - name: mac
              run: env